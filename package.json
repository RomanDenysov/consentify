{
    "name": "consentify",
    "private": true,
    "version": "0.1.0",
    "description": "Consentify monorepo: headless cookie consent SDK and adapters",
    "workspaces": [
        "packages/*"
    ],
    "packageManager": "pnpm@9",
    "scripts": {
        "build": "pnpm -w --filter @consentify/core build",
        "build:all": "pnpm -r build",
        "clean": "pnpm -r exec rimraf dist",
        "lint": "echo 'no lint configured'",
        "release": "pnpm build:all && echo 'Tag and publish from CI'"
    }
}